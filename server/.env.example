# Server config
NODE_ENV=production
PORT=3001

# Optional: Proxy for regions where Google API is blocked (Russia, etc)
# HTTPS_PROXY=http://127.0.0.1:7890

# Optional: Fallback API key (users can provide their own)
# GOOGLE_API_KEY=
